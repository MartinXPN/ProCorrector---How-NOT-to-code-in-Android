<?xml version="1.0" encoding="utf-8"?>
<shape
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- View background color -->
    <solid
        android:color="@color/suggestion_list_background" >
    </solid>

    <!-- View border color and width -->
    <stroke
        android:width="0dp"
        android:color="#14cdcdcd" >
    </stroke>

    <!-- If padding is desired
    <padding
        android:left="2dp"
        android:top="2dp"
        android:right="2dp"
        android:bottom="2dp" >
    </padding>
    -->

    <!-- Corner radius -->
    <corners
        android:radius="@dimen/rectangle_with_round_corners_radius"   >
    </corners>

</shape>